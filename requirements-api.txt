# Slim serving-only dependencies for the Docker image.
# scikit-learn / xgboost versions MUST match the training environment so the
# joblib pipeline deserialises identically (see requirements.txt).
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
scikit-learn==1.9.0
xgboost-cpu==3.3.0
pandas==2.3.3
numpy==2.5.1
joblib==1.5.3
prometheus-fastapi-instrumentator==8.0.2
