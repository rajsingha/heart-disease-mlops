# Full development environment: data science, training, tracking, serving, testing.
# Versions are pinned so a clean environment reproduces training exactly.
# scikit-learn / xgboost MUST stay in sync with requirements-api.txt so the
# serialized pipeline loads identically in the serving image.

# Data processing
pandas==2.3.3
numpy==2.5.1

# Visualization
matplotlib==3.11.0
seaborn==0.13.2

# ML frameworks
scikit-learn==1.9.0
xgboost==3.3.0

# Experiment tracking
mlflow==3.14.0

# Dataset acquisition
ucimlrepo==0.0.7

# API serving
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
prometheus-fastapi-instrumentator==8.0.2

# Model persistence
joblib==1.5.3

# Testing & quality
pytest==9.1.1
httpx==0.28.1
ruff==0.15.21

# Notebook execution
ipykernel==7.3.0
nbconvert==7.17.1
